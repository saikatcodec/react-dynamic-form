import styled from "styled-components";

const Button = styled.button`
  border: 1px solid #012ba0;
  outline: none;
  color: #000;
  padding: 0.6rem 0.9rem;
  letter-spacing: 0.2rem;
  text-transform: uppercase;
  cursor: pointer;
  border-radius: 0.2rem;
`;

export default Button;
